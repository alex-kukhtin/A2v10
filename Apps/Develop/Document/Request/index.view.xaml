<!-- Demo page -->
<Page xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Page.Toolbar>
		<Toolbar>
			<Button Icon="Plus" Tip="Создать заявку">Создать</Button>
			<Button Icon="File" Command="{BindCmd OpenSelected, Url={Bind Url}, Argument={Bind Documents}}">Открыть</Button>
			<Button Icon="Delete">Удалить</Button>
			<!--
					OpenSelected Action="Edit" -> arg из родителя (пропуская Parent) -> Action
					Create Action="Edit"
					RemoveSelected Confirm
					DbRemoveSelected Confirm
					Exec Action=Command
					Invoke Action=Command
					-->
			<Separator />
			<Button Icon="Reload" Command="{BindCmd Reload}" Tip="Обновить данные">Обновить</Button>
			<Button Icon="Requery" Command="{BindCmd Requery}" Tip="Перезагружает страницу">Перезагрузить</Button>
		</Toolbar>
	</Page.Toolbar>
	<CollectionView ItemsSource="{Bind Documents}" RunAt="ServerUrl">
		<DataGrid ItemsSource="{Bind Parent.ItemsSource}" Hover="True" 
				  Striped="True" Sort="True" GridLines="Vertical">
			<DataGrid.Pager>
				<Pager Source="{Bind Parent.Pager}" />
			</DataGrid.Pager>
			<DataGridColumn Header="Код" Content="Id" Align="Right" Fit="True"/>
			<DataGridColumn Header="Номер" Content="SNo" />
			<DataGridColumn Header="Котрагент" Content="{Bind Agent.Name}"/>
			<DataGridColumn Header="Сумма" Content="Sum" Align="Right"/>
			<DataGridColumn Header="Примечание" Content="Memo" Command="{BindCmd Open, Url='/document/request/edit'}"/>
			<DataGridColumn Header="Договор" Content="Contract.FullName" Command="{BindCmd Open, Url='/common/contract/show', Argument={Bind Contract.Id}}"/>
			<DataGridColumn Header="Contract Id" Content="Contract.Id" Command="{BindCmd Open, Url='/common/contract/show', Argument={Bind Contract.Id}}"/>
			<DataGridColumn Header="Url" Content="Url" Command="{BindCmd Open, Url={Bind Url}}"/>
		</DataGrid>
	</CollectionView>
</Page>

    