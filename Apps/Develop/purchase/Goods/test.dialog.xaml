<!-- Test index -->
<Dialog xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml">
	<Dialog.Buttons>
		<Button Content="Save" Command="{BindCmd Save}"/>
	</Dialog.Buttons>
	<Grid>
		<TextBox Label="FamilyName" Value="{Bind Donor.Person.FamilyName}"/>
	</Grid>
</Dialog>
